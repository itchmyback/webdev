Embedded JavaScript is a templating language that allows JavaScript to render HTML dynamically via JavaScript XML (JSX) on the backend.
HTML content can already be sent out with Express, but dynamic pages, HTML pages that change their content based on the user experience,
that is new, and that's the power of templating.

ejs package does not need a require statement

ejs files must be placed in a directory called "views"

css files are usually placed in a directory called "public"

syntax: enclose javascript statements inside <>
