-- ALTER TABLE employees AUTO_INCREMENT=1;
-- ALTER TABLE hr.employees
-- ADD PRIMARY KEY(employee_id);
-- must be owner of table

-- INSERT INTO employees (employee_id, first_name, last_name, email, phone_numeric, hire_date, job_id, salary, commission_pct, manager_id, department_id)
-- VALUES (868, 'Johnny', 'Nguyen', 'johnny@email', 512-555-5555, '05/12/2021', 'IT_PROG', 10, 0.1, 100, 60);

UPDATE employees
SET phone_numeric = '512-555-5555'
WHERE employee_id = 868; 

-- DELETE FROM employees WHERE employee_id = 86753;

SELECT *
FROM employees;